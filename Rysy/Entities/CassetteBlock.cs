namespace Rysy.Entities;

internal class CassetteBlock
{
    /*
     * TODO:
     * Idea #1:
     * batch render thingy, create a tile grid, use Autotiler
     * 
     * Idea #2:
     * each block will check other blocks (including itself) for neighbooring with each tile, then use Autotiler
     */
}
